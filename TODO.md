# TODO

- Fetch uploaded video metadata before enqueueing, then surface duration, resolution, frame rate, streams, and estimated output details in the UI.
- Generate or capture a lightweight preview frame near the start of the uploaded video so users can verify the source before encoding.
- Add a file size comparison view so users can compare original and encoded output size, percentage reduction, and estimated storage/bandwidth savings.
- Add a visual quality comparison view so users can compare source and encoded frames side by side, ideally with a before/after slider.
