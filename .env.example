# S3-compatible storage credentials (required)
BUCKET_KEY_ID=
BUCKET_SECRET_KEY=
BUCKET_REGION=
BUCKET_NAME=
BUCKET_ENDPOINT=

# Redis host for BullMQ (required)
REDIS_HOST=localhost

# Allowed CORS origins, comma-separated (required)
ALLOWED_ORIGINS=http://localhost:3000
